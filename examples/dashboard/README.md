# Dashboard

Complete service-dashboard template composed from public component adapters.

```sh
npm run example:dashboard
```

It demonstrates `Box`, `Text`, `Stat`, `Sparkline`, `MetricBars`, `KeyValue`,
and `ProgressBar` in one screen. Press `q` to quit.

```sh
npm run example:dashboard -- --smoke
```

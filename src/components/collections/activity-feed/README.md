# ActivityFeed

Bounded chronological event list with semantic markers.

```ts
renderActivityFeed({
  width: 40,
  height: 3,
  items: [{ id: 'deploy', label: 'Deploy finished', tone: 'success' }],
});
```

# CommandPalette

Searchable command list renderer for command surfaces.

```ts
renderCommandPalette({
  width: 50,
  height: 8,
  query: 'open',
  items: [{ id: 'open', label: 'Open file' }],
});
```

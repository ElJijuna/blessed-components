# Toolbar

`Toolbar` is a compact horizontal command group with icons, optional labels and
shortcuts, separators, disabled commands, dense mode, and width-aware overflow.

Left/Right and Shift+Tab/Tab move between enabled commands. Home/End jump to
the first/last command; Enter and Space activate it. Set `box.mouse` to `false`
to opt out of mouse activation.

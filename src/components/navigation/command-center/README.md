# CommandCenter

`CommandCenter` composes command search, recent actions, grouped results,
shortcuts, disabled state, keyboard navigation, and caller-owned execution.

```ts
commandCenter({ parent: screen, data: { items: [{ id: 'deploy', label: 'Deploy', group: 'Operations' }], recentIds: ['deploy'] } })
```

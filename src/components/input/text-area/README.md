# TextArea

Bounded multiline text renderer with optional line numbers and cursor marker.

```ts
renderTextArea({
  width: 40,
  height: 5,
  value: 'First line\nSecond line',
  lineNumbers: true,
});
```
